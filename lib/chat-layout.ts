/** Maximum width of the centered chat column (conversation + composer).
 *  Kept in one place so the message list, notices shelf, empty state, and
 *  composer cannot drift apart. */
export const CHAT_COLUMN_MAX_WIDTH = 744;

/** Base horizontal inset/padding for the chat column content. */
export const CHAT_BASE_HORIZONTAL_PADDING = 16;

/** Width reserved for the desktop scroll minimap along the right edge. */
export const CHAT_DESKTOP_MINIMAP_WIDTH = 20;
